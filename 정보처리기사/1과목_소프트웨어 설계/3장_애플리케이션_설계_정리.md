# 📝 정보처리기사 대비: 3장. 애플리케이션 설계 (Application Design)

---

## 🎯 개요 및 목표
본 장은 요구사항을 바탕으로 시스템의 전반적인 구조와 기능을 구체화하는 단계입니다. 사용자가 기대하는 기능적 요구사항(Functional Requirements)을 실제 작동하는 모듈 단위의 구조로 설계하는 것이 핵심 목표입니다.

---

## 🏛️ 1. 애플리케이션 아키텍처 설계 (Architecture Design)
시스템의 전체적인 뼈대와 구성 요소 간의 관계를 정의합니다.

*   **아키텍처 패턴 이해:**
    *   **3-Tier Architecture (3계층 구조):** 가장 일반적이며 권장되는 방식입니다.
        1.  **Presentation Layer (표현 계층):** 사용자 인터페이스 (화면). (예: 웹 브라우저, 모바일 앱)
        2.  **Application/Business Logic Layer (비즈니스 로직 계층):** 핵심 비즈니스 규칙을 처리. (실제 로직 구현)
        3.  **Data Access Layer (데이터 접근 계층):** 데이터베이스와의 통신을 전담. (SQL 실행 및 데이터 포맷팅)
    *   **N-Tier:** 필요에 따라 계층을 추가하거나 분리하는 개념.
*   **모듈화 (Modularity):** 시스템을 독립적이고 재사용 가능한 작은 기능 단위(모듈)로 분리하는 것을 의미합니다. 각 모듈은 명확한 역할과 인터페이스를 가져야 합니다.

---

## 🧱 2. 주요 설계 요소 및 활동
실제 애플리케이션을 구체화하기 위해 고려해야 할 세부 설계 요소들입니다.

### 2.1. 데이터 모델링 (Data Modeling)
*   **ERD (Entity-Relationship Diagram):** 개체(Entity), 속성(Attribute), 관계(Relationship)를 시각화합니다.
*   **정규화 (Normalization):** 데이터 중복을 최소화하고 데이터 무결성을 확보하는 과정.
    *   **1차 정규형 (1NF):** 모든 속성이 원자적 값을 가져야 함 (더 이상 쪼갤 수 없음).
    *   **2차 정규형 (2NF):** 기본 키가 아닌 속성이 기본 키의 일부에만 종속되지 않아야 함.
    *   **3차 정규형 (3NF):** 기본 키가 아닌 속성이 다른 기본 키가 아닌 속성에 종속되지 않아야 함.

### 2.2. 프로세스 및 로직 설계
*   **UML 사용:** 시스템의 동작 과정을 명세화하는 핵심 도구입니다.
    *   **Use Case Diagram:** 사용자가 시스템을 통해 수행할 수 있는 행위(Use Case)를 정의합니다. (가장 높은 수준의 요구사항 정의)
    *   **Activity Diagram:** 사용 사례의 흐름(Flow)과 프로세스의 순서를 순서도로 표현합니다.
    *   **Sequence Diagram:** 객체들이 시간 순서대로 메시지를 주고받는 순서와 타이밍을 보여줍니다. (상호작용 명세화에 유용)
*   **순서도 (Flowchart):** 프로그램의 논리적인 흐름을 기호로 표현합니다.

### 2.3. 인터페이스 설계 (Interface Design)
*   **API 명세:** 시스템 모듈 간, 또는 외부 시스템과의 통신 규약(프로토콜, 데이터 형식, 통신 방식 등)을 명확히 정의해야 합니다.
*   **사용자 인터페이스 (UI/UX 고려):** 사용자가 쉽고 직관적으로 사용할 수 있도록 화면의 레이아웃, 탐색 구조 등을 설계합니다.

---

## 💡 3. 설계 시 고려할 사항 (★시험 포인트★)
| 구분 | 고려 사항 | 중요도/설명 |
| :--- | :--- | :--- |
| **성능 (Performance)** | 트랜잭션 처리량, 응답 시간(Latency), 동시 사용자 수 예측 | 시스템 부하 테스트를 염두에 둔 아키텍처가 필요함. |
| **보안 (Security)** | 인증(Authentication), 인가(Authorization), 데이터 암호화 적용 범위 | 계층별로 접근 제어(ACL) 및 비밀번호 암호화(Hashing)가 필수적임. |
| **확장성 (Scalability)** | 향후 사용자 증가나 기능 추가에 대비한 여유 공간 설계 | 모듈 단위로 독립시켜 어느 부분이 부족해지면 그 부분만 증설할 수 있게 설계. |
| **유지보수성 (Maintainability)** | 코드 가독성, 주석 처리, 모듈 간 의존성 최소화 | 설계 문서와 코드가 분리되어 변경 시 영향 범위를 최소화해야 함. |

---
**🔑 핵심 요약 키워드:**
*   **아키텍처:** 3-Tier, 모듈화, 역할 분리
*   **데이터:** ERD, 정규화 (1NF → 2NF → 3NF), 무결성
*   **동작:** Use Case, Activity Diagram, Sequence Diagram
*   **품질:** 확장성, 보안, 성능 (성능/보안은 아키텍처 단계에서부터 고려)