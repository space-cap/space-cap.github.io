# 📝 정보처리기사 대비: 4장. 인터페이스 설계 (Interface Design)

---

## 🎯 개요 및 목표
인터페이스 설계는 **시스템의 경계(Boundary)**를 정의하는 과정입니다. 즉, 이 시스템이 **외부 세계(사용자, 다른 시스템, 데이터베이스 등)**와 어떻게, 어떤 규칙에 따라 소통할지 규정하는 것이 핵심 목표입니다. 정보처리기사 시험에서는 '규약(Protocol)'과 '명세(Specification)'의 중요성을 강조합니다.

---

## 🌐 1. 인터페이스의 유형 분류
인터페이스는 소통하는 주체에 따라 여러 유형으로 나뉩니다.

### 1.1. 사용자 인터페이스 (User Interface, UI/UX)
*   **정의:** 최종 사용자가 시스템과 직접 상호작용하는 접점입니다.
*   **설계 요소:** 화면 흐름(Flow), 사용성(Usability), 접근성(Accessibility), 정보 구조(IA).
*   **핵심 고려사항:** **사용자 경험(UX)** 중심 사고가 중요합니다. 직관적이고, 오류를 예방하는(Error Prevention) 설계가 중요합니다.

### 1.2. 시스템 간 인터페이스 (System Interface)
*   **정의:** 시스템 A와 시스템 B가 데이터를 교환하거나 기능을 호출할 때 사용되는 규약입니다. (가장 중요하게 출제되는 영역)
*   **프로토콜:** 통신 규약 (HTTP, REST, SOAP 등).
*   **API (Application Programming Interface):** 프로그램들이 서로 상호작용할 수 있도록 정의한 **규약 및 방법들의 집합체**입니다. (예: 특정 메소드 이름, 파라미터 순서, 반환 타입 등)
    *   **RESTful API:** 자원(Resource)을 중심으로 URI를 사용하며 HTTP 메소드(GET, POST, PUT, DELETE)를 활용하는 아키텍처 스타일. (최근 트렌드)
    *   **SOAP:** XML 기반의 메시징 포맷을 사용하며, 전송 과정에 복잡한 보안/보안 기능을 포함할 수 있습니다. (레거시 또는 금융권에서 사용되기도 함)

### 1.3. 데이터베이스 인터페이스
*   **정의:** 애플리케이션 계층이 데이터베이스에 접근하는 방법입니다.
*   **방법:** JDBC/ODBC 드라이버 등을 통해 SQL을 실행하거나, ORM(Object-Relational Mapping) 프레임워크를 사용하여 객체 지향적으로 접근합니다.
*   **핵심:** 데이터 접근 계층(DAL)을 명확히 분리하여 비즈니스 로직과 DB 접근 로직을 분리해야 합니다.

---

## 📜 2. 인터페이스 명세서 (Interface Specification)
인터페이스 설계의 결과물 중 가장 중요한 문서입니다. 이는 **'이 시스템은 이렇게 통신한다'**라고 모두가 동의하는 공식 문서입니다.

**💡 포함되어야 할 핵심 내용:**
1.  **통신 방식:** (예: HTTP/HTTPS)
2.  **엔드포인트(Endpoint) 주소:** (예: `api.example.com/users`) 
3.  **요청(Request) 방식:** (GET, POST 등)
4.  **요청 파라미터:** 필수/선택 여부, 데이터 타입, 형식(JSON/XML).
5.  **응답(Response) 구조:** 성공 시의 데이터 구조(Status Code 200 OK), 실패 시의 데이터 구조(Error Code, 에러 메시지).

---

## 🔄 3. 데이터 교환 및 처리 과정 (Data Exchange Flow)
| 단계 | 주체 | 활동/처리 내용 | 고려사항 (시험 포인트) |
| :--- | :--- | :--- | :--- |
| **요청 (Request)** | 클라이언트 $	o$ 서버 | 필요한 데이터를 담아 호출 (HTTP Request 전송) | **인증(Authentication)** 정보(토큰 등)를 헤더에 포함하는지 확인해야 함. |
| **처리 (Process)** | 서버 (백엔드) | 비즈니스 로직 수행 및 DB 접근을 통해 데이터 검증 및 가공. | **트랜잭션 관리**가 필수적이며, 모든 비즈니스 규칙이 적용되어야 함. |
| **응답 (Response)** | 서버 $	o$ 클라이언트 | 처리된 결과를 반환 (HTTP Response 전송) | **상태 코드(Status Code)** (200, 400, 500 등)와 함께, 요청했던 형식과 동일한 구조의 데이터가 반환되어야 함. |

---

**🔑 최종 정리 체크리스트:**
*   **UI:** 사용자 흐름 및 직관성 점검.
*   **API:** **REST** 원칙을 준수했는지, **명세서** 작성이 완벽한지 확인.
*   **데이터:** 데이터 전달 시 **규격(Format)**을 벗어나지 않는지 확인.
*   **품질:** **보안(암호화)**과 **오류 처리(예외 처리)**가 설계 단계에서 명시되어야 함.