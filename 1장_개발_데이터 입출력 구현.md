# 소프트웨어 개발: 데이터 입·출력 구현 (Information Processing Engineer Exam 대비)

## 개요

본 장에서는 소프트웨어 애플리케이션이 외부 환경(사용자, 파일 시스템, 네트워크 등)과 데이터를 주고받는 모든 과정, 즉 **데이터의 입력(Input)**과 **출력(Output)**에 관련된 구현 기법과 원리를 다룹니다. 효율적이고 안정적인 데이터 입출력 처리는 프로그램의 핵심 기능과 직결되므로, 시험에서 매우 중요한 출제 영역입니다.

---

## 1. 데이터 입력(Input) 처리

데이터를 프로그램으로 받아들이는 과정입니다. 입력 데이터의 **유효성 검사(Validation)**와 **예외 처리(Exception Handling)**가 가장 중요합니다.

### 1.1. 사용자 입력 처리 (User Input)
*   **개념:** 콘솔(CLI)이나 GUI를 통해 사용자가 직접 데이터를 입력하는 방식.
*   **핵심 고려 사항:**
    *   **자료형 검증:** 입력받은 값이 기대하는 자료형(정수, 실수, 문자열 등)인지 확인해야 합니다. (예: 숫자만 기대하는데 문자가 입력되는 경우)
    *   **범위 검증:** 데이터가 허용 가능한 최소/최대 범위를 벗어나지 않았는지 검사해야 합니다.
    *   **예외 처리:** 입력 스트림이 끊기거나, 예상치 못한 제어 문자가 들어올 경우를 대비한 로직이 필요합니다.

### 1.2. 파일 입력을 통한 데이터 처리 (File I/O)
가장 흔하게 다루는 입력 방식 중 하나입니다.

*   **파일 스트림:** 데이터를 연속적인 흐름(Stream)으로 간주하고 읽어들입니다.
*   **주요 파일 포맷:**
    *   **텍스트 파일 (Text File):** 사람이 읽을 수 있는 형태로, 일반적으로 **줄 바꿈 문자(\n)** 단위로 데이터를 분리합니다. (ASCII 또는 UTF-8 인코딩 고려)
    *   **바이너리 파일 (Binary File):** 기계가 처리하는 0과 1의 형태로, 구조화된 데이터를 저장할 때 사용합니다. (직접 메모리 구조를 매핑)
*   **입력 모드 및 함수 (언어별 차이 유의):**
    *   **순차 접근 (Sequential Access):** 파일의 시작점부터 순서대로 데이터를 읽어 나가는 방식 (가장 일반적).
    *   **검색/랜덤 접근 (Random Access):** 파일의 특정 오프셋(Offset)으로 바로 이동하여 데이터를 읽어오는 방식 (데이터 구조의 무결성을 위해 파일 구조를 명확히 정의해야 함).

### 1.3. 네트워크 입력 (Network Input)
*   **프로토콜:** HTTP, FTP, TCP/IP 등 통신 프로토콜을 통해 데이터를 수신합니다.
*   **데이터 형식:** 데이터가 메시지 단위로 캡슐화되어 오는지, 아니면 스트림 형태로 오는지 이해해야 합니다.

---

## 2. 데이터 출력(Output) 처리

프로그램이 처리한 결과를 외부로 내보내는 과정입니다.

### 2.1. 사용자 인터페이스 출력 (Display)
*   콘솔, GUI 등 최종 사용자에게 보여주는 방식입니다.
*   **핵심:** 가독성(Readability)과 일관성(Consistency) 유지가 중요합니다.

### 2.2. 파일 출력 (File Writing)
*   **추가 데이터 기록:** 기존 파일에 데이터를 덧붙이는 **'Append' 모드** 처리 방안을 숙지해야 합니다.
*   **덮어쓰기:** 파일을 생성하거나 열 때 기존 내용을 지우고 처음부터 작성하는 **'Write' 모드** 처리 방안을 숙지해야 합니다.
*   **데이터 포맷팅:** 출력할 데이터가 어떤 구조(CSV, JSON, XML 등)를 가질지 사전에 결정하고 일관되게 적용해야 합니다.

### 2.3. 네트워크 출력 (Transmission)
*   데이터를 전송할 때의 **패킷 구조**, **오류 제어(Error Control)** 메커니즘에 대한 이해가 필요합니다.

---

## 3. 구현 시 주요 고려 사항 (★핵심 정리★)

| 고려 사항 | 내용 | 시험 대비 키워드 |
| :--- | :--- | :--- |
| **데이터 무결성** | 입력받은 데이터가 논리적/물리적으로 손상되지 않도록 처리해야 합니다. | 트랜잭션 관리, 예외 전파 |
| **성능 최적화** | 대용량 데이터 처리 시 I/O 작업 자체가 병목 현상을 일으킬 수 있습니다. | 버퍼링(Buffering), 배치 처리(Batch Processing) |
| **인코딩/디코딩** | 텍스트 데이터 처리 시, 시스템 및 교환 장치 간의 문자 인코딩 불일치(예: EUC-KR vs UTF-8)를 반드시 고려해야 합니다. | Character Set, Encoding 표준 |
| **자원 해제** | 파일 핸들, 네트워크 소켓 등 시스템 자원을 사용한 후에는 **반드시 닫아주어야(Close)** 합니다. | 자원 누수(Resource Leakage) 방지 |

**📌 예상 출제 유형:**
1.  특정 파일 포맷(예: CSV)을 파싱(Parsing)하는 절차 설명.
2.  데이터 유효성 검사 로직의 누락으로 발생하는 시스템 오류 사례 제시.
3.  스트리밍 처리 시 발생하는 성능 저하 원인 및 해결책 제시.